# --- Python ---
__pycache__/
*.pyc
venv/
.venv/
.env

# --- MLflow / Tommy's tracking (kept from merge) ---
mlruns/
*.db
*.tar

# --- OS noise ---
.DS_Store

# --- record-signing keys (referee/chain.py): private key NEVER committed; .pub is safe ---
referee/keys/*.key
referee/keys/record_ed25519.key
*.ed25519.key

# --- datasets + derived data (NEVER commit; CONTRIBUTING.md: no dataset files in git) ---
data/
out/
out_guardian/
out_ped/
out_apprentice/
out_apprentice_test/
*.gguf
*.mp4
*.pt
models/nv061/results/*.log

# --- demo runtime artifacts (generated; staged.csv is force-added where intended) ---
demo/data/.target
demo/registry/
demo/models/
demo/out/

# --- fixtures (generated) ---
fixtures/smoke_25.jsonl
fixtures/detections.jsonl
fixtures/detections_real.jsonl

# --- zarf init package (376MB, fetched on demand, never committed) ---
zarf-init-*.tar.zst
zarf/dist/zarf-init-*.tar.zst

# --- deploy/ Pepr build artifacts + node deps (regenerated) ---
deploy/uds/pepr/node_modules/
deploy/uds/pepr/dist/
deploy/uds/pepr/insecure-tls-*
deploy/**/*.gz
frontend/ui/node_modules/
frontend/ui/dist/

# UDS build output (throwaway demo cosign key + large signed package — never commit)
deploy/uds/dist/
deploy/mlflow-sync/shore/
deploy/mlflow-sync/ship/
deploy/mlflow-sync/transfer/
deploy/mlflow-sync/*.log
deploy/mlflow-sync/*.pid
deploy/mlflow-sync/.venv/
deploy/mlflow-sync/out/
deploy/mlflow-sync/transfer/
*.tsbuildinfo
fleet/out/
fleet/keys/*.key
fleet/__pycache__/

# Node-3 MLflow runtime
deploy/mlflow/.venv/
deploy/mlflow/mlruns/
deploy/mlflow/mlflow.db
deploy/mlflow/mlflow.log

# Pi SSH access — credentials, NEVER commit to this public repo (local/operational only)
deploy/pi/.pi-access
deploy/pi/.pi-access.md
**/*.creds
deploy/mlflow/mlartifacts/
deploy/mlflow/.venv312/

# River trainer outputs + uv lockfile (runtime)
outputs/
uv.lock

# Emulated Pi fleet — background-feeder runtime artifacts (not source)
deploy/pi-emulation/.feed*

# Emulated ship fleet + demo API — runtime artifacts (features.*.json ARE committed)
deploy/ship-emulation/.feed*
deploy/ship-emulation/.ship_feed.log
deploy/ship-emulation/docker-compose.ddg-1*.yml   # generated per-hull composes (gen_hull.py)
demo/.api.log
